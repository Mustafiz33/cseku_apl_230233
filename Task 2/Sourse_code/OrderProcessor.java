public class OrderProcessor {
    private OrderValidator orderValidator; // Validator for ensuring order validity

    public OrderProcessor(OrderValidator orderValidator) {
        // Constructor to initialize the order processor with an order validator
    }

    public void processOrder(Order order) {
        // Validates and processes the order
        // Ensures the order meets all criteria before proceeding
    }
}
